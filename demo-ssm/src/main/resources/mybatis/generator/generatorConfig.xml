<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE generatorConfiguration PUBLIC "-//mybatis.org//DTD MyBatis Generator Configuration 1.0//EN"
        "mybatis-generator-config_1_0.dtd">
<generatorConfiguration>
    <properties resource="jdbc.properties"/>
    <classPathEntry
            location="C:/Users/318898/.m2/repository/mysql/mysql-connector-java/5.1.38/mysql-connector-java-5.1.38.jar"/>
    <context id="IER-Tables" targetRuntime="MyBatis3">
        <commentGenerator>
            <!-- 是否取消生成注释代时间戳 -->
            <property name="suppressDate" value="true"/>
            <!-- 是否取消生成注释 -->
            <property name="suppressAllComments" value="true"/>
        </commentGenerator>
        <!-- 数据库连接 -->
        <jdbcConnection driverClass="${jdbc.driverClassName}" connectionURL="${jdbc.url}"
                        userId="${jdbc.username}" password="${jdbc.password}"/>
        <javaTypeResolver>
            <!-- 是否使用bigDecimal，这里用true，false可自动转化以下类型（Long, Integer, Short, etc.） -->
            <property name="forceBigDecimals" value="true"/>
        </javaTypeResolver>
        <!-- 生成Java实体的位置 -->
        <javaModelGenerator targetPackage="com.accelerator.demo.temp.model.po"
                            targetProject="src/main/java">
            <!-- 是否让schema作为包的后缀 -->
            <property name="enableSubPackages" value="false"/>
            <!-- 从数据库返回的值被清理前后的空格  -->
            <property name="trimStrings" value="true"/>
        </javaModelGenerator>

        <!-- 生成mapper.xml的位置 -->
        <sqlMapGenerator targetPackage="mybatis.mapper"
                         targetProject="src/main/resources">
            <!-- 是否让schema作为包的后缀 -->
            <property name="enableSubPackages" value="false"/>
        </sqlMapGenerator>

        <!-- 对应的Mapper接口类文件 -->
        <javaClientGenerator type="XMLMAPPER"
                             targetPackage="com.accelerator.demo.temp.dao.mapper"
                             targetProject="src/main/java">
            <!-- 是否让schema作为包的后缀 -->
            <property name="enableSubPackages" value="false"/>
        </javaClientGenerator>

        <!-- 反向生成的表名 -->
        <table tableName="USER_" domainObjectName="User" enableUpdateByExample="false"
               enableCountByExample="false"
               enableSelectByExample="false"
               enableDeleteByExample="false">
            <!-- 是否让字段名等同数据库字段名 默认为false -->
            <!--
            例如:假设一个表包含START_DATE列
            如果这个属性是true
            那么MBG生成的属性名是START_DATE 这意味着这个值的getter和setter方法将会是getSTART_DATE()和setSTART_DATE()
            如果这个属性值是fasle
            MBG将会生成的属性名是startDate
            这意味着这个值的getter和setter方法将会是getStartDate()和setStartDate()
            -->
            <property name="useActualColumnNames" value="false"/>
            <columnOverride column="ID_" property="id"/>
        </table>
    </context>
</generatorConfiguration>